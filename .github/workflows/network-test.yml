# Generated By ChatGPT
name: Network Test

on:
  push:
    branches:
      - network-performance-test
      - network-performance-test-actions

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: dotnet restore csharp-Protoshift/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Build project
      run: dotnet build csharp-Protoshift/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj --configuration Release

    - name: Run tests
      run: dotnet test csharp-Protoshift/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj --configuration Release

    - name: Archive logs
      uses: actions/upload-artifact@v2
      with:
        name: logs
        path: logs