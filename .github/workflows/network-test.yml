# Generated By ChatGPT
name: Network Test

on:
  pull_request:
    branches:
      - network-performance-test
      - kcp-proxy-packet-disorder-fix
      - kcp-proxy-packet-disorder-fix-1-bug-testing
  push:
    branches:
      - network-performance-test
      - kcp-proxy-packet-disorder-fix-1-bug-testing

jobs:
  network-benchmark-windows-rel:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: dotnet restore KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: cd KcpTests/KcpPerformanceTest ; dotnet run --configuration Release

    - name: Archive logs
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (windows-rel) - ${{ github.sha }}
        path: KcpTests/KcpPerformanceTest/logs

  network-benchmark-ubuntu-rel:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: dotnet restore KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: cd KcpTests/KcpPerformanceTest ; dotnet run --configuration Release

    - name: Archive logs
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (ubuntu-rel) - ${{ github.sha }}
        path: KcpTests/KcpPerformanceTest/logs
