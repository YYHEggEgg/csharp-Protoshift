# Generated By ChatGPT
name: Network Test

on:
  pull_request:
    branches:
      - main
      - development

jobs:
  network-proxy-benchmark-windows-rel:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: cd KcpTests/KcpPerformanceTest ; dotnet run --configuration Release -- --packet-size 3500 --packet-repeat-time 5000 --packet-interval 40 --github-actions --clients-count 2 --packet-delay-log

    - name: Archive logs
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-windows-rel) - ${{ github.sha }}
        path: KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  network-proxy-benchmark-ubuntu-rel:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: cd KcpTests/KcpPerformanceTest ; dotnet run --configuration Release -- --packet-size 3500 --packet-repeat-time 5000 --packet-interval 40 --github-actions --clients-count 2 --packet-delay-log

    - name: Archive logs
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-ubuntu-rel) - ${{ github.sha }}
        path: KcpTests/KcpPerformanceTest/logs
        retention-days: 21
        
  network-directly-connect-benchmark-windows-rel:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      # Generated by ChatGPT
      run: cd KcpTests/KcpPerformanceTest ; dotnet run --configuration Release --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY -- --packet-size 3500 --packet-repeat-time 5000 --packet-interval 40 --github-actions --clients-count 2 --packet-delay-log

    - name: Archive logs
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (directly-windows-rel) - ${{ github.sha }}
        path: KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  network-directly-connect-benchmark-ubuntu-rel:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      # Generated by ChatGPT
      run: cd KcpTests/KcpPerformanceTest ; dotnet run --configuration Release --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY -- --packet-size 3500 --packet-repeat-time 5000 --packet-interval 40 --github-actions --clients-count 2 --packet-delay-log

    - name: Archive logs
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (directly-ubuntu-rel) - ${{ github.sha }}
        path: KcpTests/KcpPerformanceTest/logs
        retention-days: 21
