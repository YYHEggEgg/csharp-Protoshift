# Generated By ChatGPT
name: Network Test

on:
  pull_request:
    branches:
      - network-performance-test
      - network-performance-test-actions
  push:
    branches:
      - network-performance-test
      - network-performance-test-actions

jobs:
  network-benchmark-debug:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: dotnet restore KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: cd KcpTests/KcpPerformanceTest ; dotnet run --configuration Debug

    - name: Archive logs
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (debug) - ${{ github.sha }}
        path: KcpTests/KcpPerformanceTest/logs

  network-benchmark-release:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: dotnet restore KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: cd KcpTests/KcpPerformanceTest ; dotnet run --configuration Release

    - name: Archive logs
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (rel) - ${{ github.sha }}
        path: KcpTests/KcpPerformanceTest/logs