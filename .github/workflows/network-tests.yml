# Generated By ChatGPT
name: Network Test

on:
  pull_request:
    branches:
      - main
      - development
  push:
    branches:
      - main
      - development
      - byte-check-mode

jobs:

  # Quick Check

  net-debug-direct-ubuntu:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Debug
        --property:DefineConstants=TRACE%3bDEBUG%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY --
        --packet-size 3500
        --packet-repeat-time 200
        --packet-interval 40
        --github-actions
        --clients-count 1
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (directly-ubuntu-debug)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-debug-direct-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Debug
        --property:DefineConstants=TRACE%3bDEBUG%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLYP%3bBYTE_CHECK_MODE%3bKCP_INNER_LOG --
        --packet-size 3500
        --packet-repeat-time 200
        --packet-interval 40
        --github-actions
        --clients-count 1
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (direct-ubuntu-debug, BYTE_CHECK_MODE)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-debug-direct-corrupt-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Debug
        --property:DefineConstants=TRACE%3bDEBUG%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE%3bCONNECT_SERVERONLY%3bCORRUPT_PACKET%3bKCP_INNER_LOG --
        --packet-size 3500
        --packet-repeat-time 200
        --packet-interval 40
        --github-actions
        --clients-count 1
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (direct-ubuntu-debug, BYTE_CHECK_MODE, CORRUPT_PACKET)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-debug-proxy-ubuntu:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Debug --
        --packet-size 3500
        --packet-repeat-time 200
        --packet-interval 40
        --github-actions
        --clients-count 1
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-ubuntu-debug)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-debug-proxy-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Debug
        --property:DefineConstants=TRACE%3bDEBUG%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE%3bKCP_INNER_LOG --
        --packet-size 3500
        --packet-repeat-time 200
        --packet-interval 40
        --github-actions
        --clients-count 1
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-ubuntu-debug, BYTE_CHECK_MODE)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-debug-proxy-corrupt-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Debug
        --property:DefineConstants=TRACE%3bDEBUG%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE%3bCORRUPT_PACKET%3bKCP_INNER_LOG --
        --packet-size 3500
        --packet-repeat-time 200
        --packet-interval 40
        --github-actions
        --clients-count 1
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-ubuntu-debug, BYTE_CHECK_MODE, CORRUPT_PACKET)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  # Basic (rel)

  net-rel-direct-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (directly-ubuntu-rel)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-direct-windows:
    runs-on: windows-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (directly-windows-rel)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-proxy-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-ubuntu-rel)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-proxy-windows:
    runs-on: windows-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-windows-rel)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  # Byte Check Mode (rel)

  net-rel-direct-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY%3bBYTE_CHECK_MODE --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (directly-ubuntu-rel, BYTE_CHECK_MODE)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-direct-bytecheck-windows:
    runs-on: windows-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY%3bBYTE_CHECK_MODE --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (directly-windows-rel, BYTE_CHECK_MODE)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-proxy-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-ubuntu-rel, BYTE_CHECK_MODE)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-proxy-bytecheck-windows:
    runs-on: windows-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-windows-rel, BYTE_CHECK_MODE)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-direct-corrupt-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-direct-corrupt-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu
      - net-debug-proxy-corrupt-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY%3bBYTE_CHECK_MODE%3bCORRUPT_PACKET --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (directly-ubuntu-rel, BYTE_CHECK_MODE, CORRUPT_PACKET)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-direct-corrupt-bytecheck-windows:
    runs-on: windows-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-direct-corrupt-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu
      - net-debug-proxy-corrupt-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY%3bBYTE_CHECK_MODE%3bCORRUPT_PACKET --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (directly-windows-rel, BYTE_CHECK_MODE, CORRUPT_PACKET)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-proxy-corrupt-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-direct-corrupt-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu
      - net-debug-proxy-corrupt-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE%3bCORRUPT_PACKET --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-ubuntu-rel, BYTE_CHECK_MODE, CORRUPT_PACKET)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-proxy-corrupt-bytecheck-windows:
    runs-on: windows-latest

    needs:
      - net-debug-direct-ubuntu
      - net-debug-direct-bytecheck-ubuntu
      - net-debug-direct-corrupt-bytecheck-ubuntu
      - net-debug-proxy-ubuntu
      - net-debug-proxy-bytecheck-ubuntu
      - net-debug-proxy-corrupt-bytecheck-ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE%3bCORRUPT_PACKET --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (proxy-windows-rel, BYTE_CHECK_MODE, CORRUPT_PACKET)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  # Byte Check Mode Extended Test

  net-rel-direct-bigdata-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-rel-direct-bytecheck-ubuntu
      - net-rel-direct-bytecheck-windows
      - net-rel-proxy-bytecheck-ubuntu
      - net-rel-proxy-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY%3bBYTE_CHECK_MODE --
        --packet-size 30000
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

  net-rel-direct-bigdata-bytecheck-windows:
    runs-on: windows-latest

    needs:
      - net-rel-direct-bytecheck-ubuntu
      - net-rel-direct-bytecheck-windows
      - net-rel-proxy-bytecheck-ubuntu
      - net-rel-proxy-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY%3bBYTE_CHECK_MODE --
        --packet-size 30000
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

  net-rel-proxy-bigdata-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-rel-direct-bytecheck-ubuntu
      - net-rel-direct-bytecheck-windows
      - net-rel-proxy-bytecheck-ubuntu
      - net-rel-proxy-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE --
        --packet-size 30000
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

  net-rel-proxy-bigdata-bytecheck-windows:
    runs-on: windows-latest

    needs:
      - net-rel-direct-bytecheck-ubuntu
      - net-rel-direct-bytecheck-windows
      - net-rel-proxy-bytecheck-ubuntu
      - net-rel-proxy-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE --
        --packet-size 30000
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

  net-rel-direct-corrupt-bigdata-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-rel-direct-bytecheck-ubuntu
      - net-rel-direct-bytecheck-windows
      - net-rel-proxy-bytecheck-ubuntu
      - net-rel-proxy-bytecheck-windows
      - net-rel-direct-corrupt-bytecheck-ubuntu
      - net-rel-direct-corrupt-bytecheck-windows
      - net-rel-proxy-corrupt-bytecheck-ubuntu
      - net-rel-proxy-corrupt-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY%3bBYTE_CHECK_MODE%3bCORRUPT_PACKET --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

  net-rel-direct-corrupt-bigdata-bytecheck-windows:
    runs-on: windows-latest

    needs:
      - net-rel-direct-bytecheck-ubuntu
      - net-rel-direct-bytecheck-windows
      - net-rel-proxy-bytecheck-ubuntu
      - net-rel-proxy-bytecheck-windows
      - net-rel-direct-corrupt-bytecheck-ubuntu
      - net-rel-direct-corrupt-bytecheck-windows
      - net-rel-proxy-corrupt-bytecheck-ubuntu
      - net-rel-proxy-corrupt-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bCONNECT_SERVERONLY%3bBYTE_CHECK_MODE%3bCORRUPT_PACKET --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

  net-rel-proxy-corrupt-bigdata-bytecheck-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-rel-direct-bytecheck-ubuntu
      - net-rel-direct-bytecheck-windows
      - net-rel-proxy-bytecheck-ubuntu
      - net-rel-proxy-bytecheck-windows
      - net-rel-direct-corrupt-bytecheck-ubuntu
      - net-rel-direct-corrupt-bytecheck-windows
      - net-rel-proxy-corrupt-bytecheck-ubuntu
      - net-rel-proxy-corrupt-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE%3bCORRUPT_PACKET --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

  net-rel-proxy-corrupt-bigdata-bytecheck-windows:
    runs-on: windows-latest

    needs:
      - net-rel-direct-bytecheck-ubuntu
      - net-rel-direct-bytecheck-windows
      - net-rel-proxy-bytecheck-ubuntu
      - net-rel-proxy-bytecheck-windows
      - net-rel-direct-corrupt-bytecheck-ubuntu
      - net-rel-direct-corrupt-bytecheck-windows
      - net-rel-proxy-corrupt-bytecheck-ubuntu
      - net-rel-proxy-corrupt-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE%3bCORRUPT_PACKET --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 40
        --github-actions
        --clients-count 2
        --packet-delay-log

  # Pressure Test

  net-rel-pressure-10-clients-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-rel-direct-ubuntu
      - net-rel-direct-windows
      - net-rel-proxy-ubuntu
      - net-rel-proxy-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj
    
    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 50
        --github-actions
        --clients-count 10

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (ubuntu-rel-pressure-x10)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-pressure-20-clients-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-rel-direct-ubuntu
      - net-rel-direct-windows
      - net-rel-proxy-ubuntu
      - net-rel-proxy-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 50
        --github-actions
        --clients-count 10

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (ubuntu-rel-pressure-x20)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-pressure-bytecheck-10-clients-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-rel-direct-ubuntu
      - net-rel-direct-windows
      - net-rel-proxy-ubuntu
      - net-rel-proxy-windows
      - net-rel-direct-bytecheck-ubuntu
      - net-rel-direct-bytecheck-windows
      - net-rel-proxy-bytecheck-ubuntu
      - net-rel-proxy-bytecheck-windows
      - net-rel-direct-bigdata-bytecheck-ubuntu
      - net-rel-direct-bigdata-bytecheck-windows
      - net-rel-proxy-bigdata-bytecheck-ubuntu
      - net-rel-proxy-bigdata-bytecheck-windows
      - net-rel-direct-corrupt-bigdata-bytecheck-ubuntu
      - net-rel-direct-corrupt-bigdata-bytecheck-windows
      - net-rel-proxy-corrupt-bigdata-bytecheck-ubuntu
      - net-rel-proxy-corrupt-bigdata-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj
    
    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 50
        --github-actions
        --clients-count 10

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (ubuntu-rel-pressure-x10, BYTE_CHECK_MODE)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21

  net-rel-pressure-bytecheck-20-clients-ubuntu:
    runs-on: ubuntu-latest

    needs:
      - net-rel-direct-bigdata-bytecheck-ubuntu
      - net-rel-direct-bigdata-bytecheck-windows
      - net-rel-proxy-bigdata-bytecheck-ubuntu
      - net-rel-proxy-bigdata-bytecheck-windows
      - net-rel-direct-corrupt-bigdata-bytecheck-ubuntu
      - net-rel-direct-corrupt-bigdata-bytecheck-windows
      - net-rel-proxy-corrupt-bigdata-bytecheck-ubuntu
      - net-rel-proxy-corrupt-bigdata-bytecheck-windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Restore NuGet packages
      run: |
        dotnet nuget add source https://apiint.nugettest.org/v3/index.json -n "nuget.org (Integration)"
        dotnet restore Tests/KcpTests/KcpPerformanceTest/KcpPerformanceTest.csproj

    - name: Run tests
      run: >
        cd Tests/KcpTests/KcpPerformanceTest ;
        dotnet run --configuration Release
        --property:DefineConstants=TRACE%3bRELEASE%3bNET%3bNET6_0%3bNETCOREAPP%3bMIHOMO_KCP%3bBYTE_CHECK_MODE --
        --packet-size 3500
        --packet-repeat-time 5000
        --packet-interval 50
        --github-actions
        --clients-count 10

    - name: Archive logs
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: Report & logs (ubuntu-rel-pressure-x20, BYTE_CHECK_MODE)
        path: Tests/KcpTests/KcpPerformanceTest/logs
        retention-days: 21